# Brand assets

Drop the Actprove lockup here as `actprove.svg` - chevron plus wordmark, white
on transparent.

Until it is present the header draws the chevron inline. The console never ships
a broken image.
